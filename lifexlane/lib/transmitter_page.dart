import 'package:flutter/material.dart';

class TransmitterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Transmitter Page'),
      ),
      body: const Center(
        child: Text('This is the transmitter page.'),
      ),
    );
  }
}
